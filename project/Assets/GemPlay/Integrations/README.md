# GemPlay.Features

This provides an interface to GemPlay's Features, using all of GemPlay's independent Functions.

`FeatureInterface` internally connects Features to Interactables, such as GUI buttons, via the appropriate event systems.
